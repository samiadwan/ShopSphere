import type { Action, ActionReducer } from '@ngrx/store';
export interface FeaturesProps {
    features: Record<string, unknown>;
}
type ActionCheck = (action: Action) => action is Action & FeaturesProps;
export declare const createMergeReducer: (actionCheck: ActionCheck) => <T = unknown, V extends Action<string> = Action<string>>(reducer: ActionReducer<T, V>) => (state: T, action: V) => T;
export {};
